import "./globals.css";

export const metadata = { title: "HKVT Volunteer Allocation", description: "Live volunteer registration and service allocation" };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
